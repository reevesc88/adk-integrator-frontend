interface:
  display_name: "Adk Integrator Frontend"
  short_description: "Repo-specific patterns and workflows for adk-integrator-frontend"
  default_prompt: "Use the adk-integrator-frontend repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true